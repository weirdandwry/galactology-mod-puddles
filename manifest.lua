-- All mods must have a manifest.lua file in their root folder, with the
-- following structure.
return {
    name = "Shower puddles (example mod)",
    description = "Example mod that adds water puddles around showers, which can cause anybody walking by to slip and slide.",
    -- This field is mandatory and for now it has to be 4.
    api_version = 4
}
